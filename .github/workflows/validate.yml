name: CI

on:
  pull_request:
    branches:
    - travis-ci

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: build/ci/install-deps

