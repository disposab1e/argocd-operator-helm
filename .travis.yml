dist: xenial
language: python
os: linux

python:
- "3.6"

if: branch = travis-ci

before_install:
- build/ci/install-vm-driver

#install:
#- build/ci/start-minikube
#- build/ci/install-olm

jobs:
  include:
  - name: build-and-test
    script:
    - build/ci/hello
